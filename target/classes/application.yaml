server:
  port: 2013

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/billing_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

purchase:
  service:
    url: http://localhost:2021
