server:
  port: 2013

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/billing_db
    username: root
    password: ellenverma
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

purchase:
  service:
    url: http://localhost:2021
